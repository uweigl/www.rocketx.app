RocketX site bundle
===================
1. Run once:   python3 get_assets.py     (downloads the 8 media files into assets/)
2. Open index.html, or upload the whole folder (index.html + assets/) to your host.

After step 1 the site no longer depends on the Framer CDN.
Fonts still load from Google Fonts; everything else is local.
